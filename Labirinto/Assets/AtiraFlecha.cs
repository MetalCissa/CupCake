using UnityEngine;
using System.Collections;

public class AtiraFlecha : MonoBehaviour {
	public GameObject flecha;
	public float tempo;
	public GameObject pontoDeSaida;

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
//		tempo += Time.deltaTime;
//		if(tempo >= 0.5){
//			GameObject t = Instantiate(flecha, pontoDeSaida.GetComponent<Transform>().position, Quaternion.identity) as GameObject;
//			tempo = 0;
//		}
	}
}
